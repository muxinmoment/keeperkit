# Step 01 结构视图底座

这一步先把模组结构视图做出来了。

## 已完成

- 新增模组结构视图接口。
- 按场景、NPC、线索、地点、时间线等内容做轻量分组。
- 新增对应测试。

## 现在能做什么

- 调用 `GET /api/v1/modules/{module_id}/structure` 查看模组结构。
- 看到每个结构组的文件来源和预览。

## 下一步

- 把结构视图接到前端。
- 让前端可以直接切换到结构浏览。
- 再继续做时间线整理的独立视图。
