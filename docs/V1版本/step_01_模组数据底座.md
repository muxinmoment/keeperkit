# Step 01 模组数据底座

这一步先把模组的基础结构立起来了。

## 已完成

- 新增 `backend/data/modules/private/` 作为模组私有目录。
- 新增模组元数据模型和目录约定。
- 新增模组列表、创建、详情 API。
- 新增基础测试，确认模组可以创建并列出。

## 现在能做什么

- 先创建一个模组目录。
- 读取 `module.json`。
- 在后端列出已有模组。

## 下一步

- 支持上传文件。
- 把 Markdown/TXT/PDF 放进模组目录并建立索引。
- 让前端能选择当前模组并开始问答。
