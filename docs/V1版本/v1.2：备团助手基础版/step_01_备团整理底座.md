# Step 01 备团整理底座

这一步先把备团整理能力接上了。

## 已完成

- 新增时间线接口。
- 新增备团摘要接口。
- 支持从模组文件里轻量提取场景、线索、NPC 和触发信息。

## 现在能做什么

- 调用 `GET /api/v1/modules/{module_id}/timeline` 查看时间线。
- 调用 `GET /api/v1/modules/{module_id}/prep-summary` 查看备团摘要。

## 下一步

- 把时间线和备团摘要接到前端。
- 给前端增加明确的“备团助手”入口。
- 继续提升抽取质量。
